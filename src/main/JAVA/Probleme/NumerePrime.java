package main.JAVA.Probleme;

import javax.imageio.stream.ImageInputStream;
/*
public class NumerePrime {
    public static void Prime(int nrs) {
                int count = 0;
        List<Integer> nrs = new ArrayList<>();
            nrs.add(i);
        System.out.println(nrs.get(i));

        public static boolean isPrime (int i) {
            for (int i = 0; i <= 100; i++) {
                if (isPrime(i)) {
                    return false;
                } else
                    return true;
            }
            for (int j = 2; j <= i / 2; j++) {
                if (i % j != 0) {
                    System.out.println("nr prim " + i);
                    count++;
                }

                if (count == 10) {
                    break;
                }

            }
        }

    }

    public static void main(String[] args) {
        Prime(100);
            }
}

*/


/* Create a method that returns total prime numbers until the count is 10
// with a for statement using any range of numbers, n given
// Determine if the number is a prime number using the isPrime method
// if it is a prime number, print it out AND increment a count of the number of prime numbers found
// if that count is 10 exit the for loop
// hint:  Use the break; statement to exit */